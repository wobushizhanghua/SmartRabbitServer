#include "boot.h"
#include "svr_connection.h"

int rbtF_boot( rabbit * r )
{
	if(rbtNet_listen(r, G(r)->port) < 0) {
		kLOG(r, 0, "[Error] 监听端口: %d 失败 !\n", G(r)->port);
		exit(1);
	}

	kLOG(r, 0, "[LOG] 初始化成功！监听端口：%d !\n", G(r)->port);

	return 0;
}

